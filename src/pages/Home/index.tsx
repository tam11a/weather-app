import { Button } from "antd";

const Home = () => {
	return (
		<>
			<Button
				type="primary"
				className="rounded-full"
			>
				Hello!!
			</Button>
			<button className="rounded-full">Save Changes</button>
		</>
	);
};

export default Home;
